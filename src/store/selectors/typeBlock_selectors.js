export const active = (state) => state.typeBlock.active;
export const all = (state) => state.typeBlock;
export const activeLinkModal = (state) => state.typeBlock.activeLinkModal;
export const activeImageModal = (state) => state.typeBlock.activeImageModal;
